// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig : {
    apiKey: "AIzaSyCvAmxn1NH4V28v4s71avDq_cmwrEaU-qs",
    authDomain: "restaurantranda.firebaseapp.com",
    databaseURL: "https://restaurantranda.firebaseio.com",
    projectId: "restaurantranda",
    storageBucket: "restaurantranda.appspot.com",
    messagingSenderId: "713922904287",
    appId: "1:713922904287:web:b8ebb9a836bc84df"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
